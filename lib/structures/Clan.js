const Base = require('./Base');

class Clan extends Base {

    constructor(data) {
        super();

        this.patch(data);
    }

    patch(data) { // eslint-disable-line
    }

}

module.exports = Clan;
